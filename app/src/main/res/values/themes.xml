<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.TrackFunds" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- opsional: transparansi status/nav bar untuk enableEdgeToEdge() -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.TrackFunds.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.TrackFunds</item>
    </style>

</resources>